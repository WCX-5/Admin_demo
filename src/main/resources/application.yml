server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/admin_demo?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&useSSL=false
    username: root
    password: 12345678
  thymeleaf:
    cache: false # 开发时关闭缓存，方便页面修改

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath:mapper/*.xml

# JWT 配置
jwt:
  secret: admin_demo1SecretKey20241111111111111111111111111111 # 自己改长一点
  expiration: 86400000 # 1天，单位毫秒